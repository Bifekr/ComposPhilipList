<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposPhilipList"
        tools:targetApi="31">
        <activity
            android:name=".playlist.ModifierOffsetPadding"
            android:exported="false"
            android:label="@string/title_activity_modifer_offset_padding"
            android:theme="@style/Theme.ComposPhilipList" />
        <activity
            android:name=".playlist.ImageCardComposable"
            android:exported="false"
            android:label="@string/title_activity_image_card_composabe"
            android:theme="@style/Theme.ComposPhilipList" />
        <activity
            android:name=".playlist.MainWorking"
            android:exported="true"
            android:label="@string/title_activity_part3"
            android:theme="@style/Theme.ComposPhilipList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposPhilipList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>